{
  "name": "pet-care-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "channel-simulator"
  ],
  "scripts": {
    "dev": "concurrently --names client,server,simulator --prefix-colors cyan,green,yellow \"npm run dev --workspace=client\" \"npm run dev --workspace=server\" \"npm run dev --workspace=channel-simulator\"",
    "build": "npm run build --workspace=server && npm run build --workspace=channel-simulator && npm run build --workspace=client",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
